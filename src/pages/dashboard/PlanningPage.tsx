import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

const PlanningPage = () => {
  return (
    <div className="container mx-auto">
      <Card>
        <CardHeader>
          <CardTitle>Planificación</CardTitle>
        </CardHeader>
        <CardContent>
          <p>Esta sección te permitirá crear, ver y gestionar tus planificaciones de clase, unidades didácticas y calendarios académicos.</p>
          {/* El contenido detallado de esta página se implementará a continuación. */}
        </CardContent>
      </Card>
    </div>
  );
};

export default PlanningPage;